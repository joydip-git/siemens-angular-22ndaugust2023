/*
//common js format
const add = (a, b) => a + b
const subtract = (a, b) => a - b

module.exports = {
    add: add,
    subtract: subtract
}
*/
//ES6 module format
export const add = (a, b) => a + b
export const subtract = (a, b) => a - b
